# mcp 2.0.0 removed mcp.server.fastmcp, which server.py imports. Capped
# until the server is ported to the 2.x MCPServer API.
mcp>=1.10.0,<2
uvicorn>=0.30.0
starlette>=0.40.0
python-dotenv>=1.0.0
httpx>=0.27.0
psutil>=5.9.0
pyautogui>=0.9.54
pillow>=10.0.0
